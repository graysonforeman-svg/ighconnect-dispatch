# Railway → ighconnect-dispatch service → Variables
# Required at build time (NEXT_PUBLIC_* are baked into the client bundle).

NEXT_PUBLIC_API_URL=https://igh-connect-production.up.railway.app
NEXT_PUBLIC_WS_URL=https://igh-connect-production.up.railway.app

# Do not set PORT — Railway assigns it automatically.
